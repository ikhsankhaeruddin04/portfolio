import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",       // agar bisa export static HTML
  // basePath: "/portfolio", // optional, sesuai nama repo GitHub
};

export default nextConfig;
